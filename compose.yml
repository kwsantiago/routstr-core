version: '3.8'

services:
  routstr-proxy:
    build: .
    command: ["fastapi", "dev", "main.py", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
